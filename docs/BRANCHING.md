# Branching strategy

This lab uses **GitHub Flow**:

- `main` is always deployable
- Feature branches: `docs/*`, `chore/*`, `fix/*`
- One PR = one focused change
- Merge with a merge commit (keeps PR history for Pull Shark practice)
